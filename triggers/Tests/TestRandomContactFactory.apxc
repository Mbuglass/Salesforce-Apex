@isTest
public class TestRandomContactFactory {
	@isTest
    static void testNames(){
        List<Contact> con = RandomContactFactory.generateRandomContacts(10, 'Smith');
        System.assertEquals(10, con.size());
    }
}